{
  "name": "@fortunel/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "generate": "echo 'Phase A — drizzle-kit generate'",
    "migrate": "echo 'Phase A — drizzle-kit migrate'",
    "studio": "echo 'Phase A — drizzle-kit studio'",
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "@fortunel/config": "workspace:*"
  }
}
