{
  "name": "@fortunel/shared-types",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "codegen": "echo 'Phase A — Zod → JSON Schema → Pydantic codegen'",
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "@fortunel/config": "workspace:*"
  }
}
