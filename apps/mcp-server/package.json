{
  "name": "@fortunel/mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "fortunel-mcp": "dist/stdio.js"
  },
  "scripts": {
    "dev": "echo 'Phase B — MCP scaffold not yet generated'",
    "build": "echo 'Phase B — MCP scaffold not yet generated'",
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "@fortunel/config": "workspace:*"
  }
}
