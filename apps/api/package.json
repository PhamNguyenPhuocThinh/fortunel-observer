{
  "name": "@fortunel/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@fortunel/db": "workspace:*",
    "@fortunel/shared-types": "workspace:*",
    "@hono/zod-openapi": "0.16.0",
    "@noble/hashes": "^2.2.0",
    "@scalar/hono-api-reference": "^0.10.0",
    "better-auth": "~1.6.11",
    "drizzle-orm": "^0.36.4",
    "hono": "^4.11.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@fortunel/config": "workspace:*",
    "@scalar/openapi-parser": "^0.28.0",
    "wrangler": "^4.18.0"
  }
}
