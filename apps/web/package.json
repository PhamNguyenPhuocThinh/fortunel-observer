{
  "name": "@fortunel/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo 'Phase C — Astro scaffold not yet generated'",
    "build": "echo 'Phase C — Astro scaffold not yet generated'",
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "@fortunel/config": "workspace:*"
  }
}
